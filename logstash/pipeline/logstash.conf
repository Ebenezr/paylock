input {
  http {
    port => 5001
    codec => json
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "paylock-logs-%{+YYYY.MM.dd}"
  }
  stdout {
    codec => rubydebug
  }
}